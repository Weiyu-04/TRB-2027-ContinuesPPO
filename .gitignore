__pycache__/
*.pyc
*.pyo
.pytest_cache/
.DS_Store

# LaTeX 编译产物（tex 在，随时可重编；别进仓库）
*.aux
*.log
*.out
# 🔴 2026-08-01：上面那条 `*.log` 曾把**训练日志**一起静默吞掉（user 回传 结果auto-3 时，
#   `_F240*.log` 一个都没进仓库，导致「log 说跑完了 / progress.json 说没跑完」这条线索
#   在仓库这边根本查不了）。训练日志是证据、不是编译产物 ⟹ 显式放行。
!TRB/Paper/正式实验/**/*.log
!TRB/结果/**/*.log
*.toc
*.bbl
*.blg
*.synctex.gz
*.fdb_latexmk
*.fls
# xelatex 的原始编译输出（临时）；跟踪的是 *_预览.pdf（人工更新的仓库预览版）
TRB/Paper/01_论文稿/729-paper-中文版.pdf
TRB/Paper/01_论文稿/801-paper-英文版.pdf

# 仓库根目录保持干净：论文 PDF 只在 TRB/Paper/01_论文稿/ 下生成一份
/*.pdf
/*.tex
/*.aux
