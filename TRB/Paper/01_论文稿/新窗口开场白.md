# 新窗口开场白（贴给下一个 Claude 用）

> 这份是**给 user 复制粘贴用的**。下面「────」之间的全文直接贴进新窗口即可。
> 写作原则：只给**指针 + 红线 + 顺序**，不复述细节（细节在 `02`/`03` 里，让新窗口自己去读、自己去核）。

────────────────────────────────────────────────────────────

接手 TRB 2027 项目（连续动作空间下可证明 COLREGs 方向合规的无人船避碰，投影式安全盾 + PPO）。

【第一步】按 `TRB/CLAUDE.md` §5 的顺序读文档：CLAUDE.md → 01 → 02 → 03 → 04。
读 02 时**重点看「🤝 工作交接（2026-07-30 · 论文中文稿窗口收尾）」那一节**，它是上个窗口专门为你写的，
里面有完整的账：做完了什么、剩下什么、我的判断是什么、哪些是我核过的、哪些还没核。
03 的相关段落是 **L243-续12（含补记 1–7）**。仓库分支 `main`，工作树应当干净。

【现在的状态】
· 4 台机器正在跑正式实验（9 种配置 × 8 颗种子 × 1016 万步），user 估 31 号上午跑完。
· 🔴🔴 **铁律：训练期间绝对不许改 `代码/` 里的任何 `.py`/`.sh`**。树指纹是"这张表由同一套代码跑出来"
  的凭证，而每条 run 起一个新 python 进程 ⟹ 中途改文件会让前后两批 run 变成两个代码版本。
  发现代码层问题**记进 03，别动手**。只准改 `02`/`03`/`04` 和 `Paper/`（它们不在指纹里）。
  两个指纹：`TREE=c823ccfedda851a1` · **`TRAIN=198d2c8a61c8f8ba`（论文方法节要引的是这个）**。
· 论文中文稿 = `Paper/01_论文稿/729-paper-中文版.tex`（20 页 · 编译 0 错误 0 缺字）。
  方法论与实验设计**已写实**；结果 / 讨论 / 结论是**骨架 + 114 处待填 + 6 张白框图**。
  🔴 **`Paper/trb2027_main.tex` 与 `_留档_0626_*.tex` 都是过时旧稿，别照它们改。**

【你的任务，按顺序】

一、**先做独立复审**（别一上来就写）
把上个窗口的**分析、判断、解读、以及它声称完成的工作**复审一遍。**别重跑已经做完的**——
02 交接节 §二 列了它做了什么、§三 列了它的 5 条结论性判断。你要做的是：
  ⏳1 **抽查它"用实跑代码核过方法节数字"这一条是不是真的**。它说导入 `trb_env` 真模块对账了 22 项。
     你自己挑 5~6 个数（尤其命题 1 的 764.16 / 网格多转 37.5% / `ω<0=右转` 的符号约定）回代码核。
  ⏳2 **核那 33 项方法论环节对账**是不是真的 33/33。它用的是脚本化字串命中，**这种检查会有假阴/假阳**
     （它自己就踩过一次：用 `split('%')` 切注释，`\%` 被误判成注释起点，导致行尾 2000 字既没检查也没处理）。
     你至少抽 5 项自己回论文和代码两头核。
  ⏳3 **对 §三 那 5 条结论性判断表态**：同意就说同意，不同意就直说，别顺着说。
     尤其第 1 条（命题不该单独成节）和第 2 条（多他船推广该砍）——这两条改动了论文结构，值得你自己判一遍。
  ⏳4 02 交接节 §六 有一个论文复审 workflow 的结论。**核它的发现有没有被正确落地**（或正确地判为不成立）。
🔴 **复审要省着花 token**：目标不是重做一遍，是抽查承重结论。发现对不上的直接说。

二、**用大白话把方法论讲给 user 听**（这是 user 明确要求的，优先级高于继续写）
user 原话：「我现在还没有仔细看方法论的部分，我不知道里面写的内容是什么意思。可能还需要新窗口给我解释一下。」
⟹ 读完 `Paper/01_论文稿/729-paper-中文版.tex` 的 Methodology 全节，用**通俗中文**逐小节讲清：
这一节在讲什么、为什么这样设计、每条"性质"到底保证了什么、边界在哪。
**别堆术语和英文缩写**（`CLAUDE.md` §2 是硬要求）。让 user 先确认"我们要讲的方法本身对不对"，再谈加厚。

三、**继续写论文**（user 指定的下一步，四件）
1. **文献综述 + 引言**。拿对标论文综述过的文献 + 补 2025–2026 年的，**共约 40 篇**。
   ⚠️ 引用格式 = **Chicago 作者-年份**（正文 `(Smith 2020)`、文末按姓氏字母序、悬挂缩进、**不许编号**）。
   稿子里已备好 `\cit{}` / `\citp{}` 宏。
2. **方法论加厚**。user 觉得偏精简、对标论文公式丰富。02 交接节 §七 有上个窗口给的 5 条具体建议
   （最该补的是**相遇态势判定的谓词化**和**单步无碰约束的显式推导**）——**你自己判断是否采纳**。
3. **强化学习部分补什么**。同上 §七 有 3 条建议（带盾 MDP 的形式化 / Beta 参数化与众数式子 /
   回报中与盾耦合的项）。方向是补**与本文机制耦合的部分**，不是讲 PPO 是什么。
4. **查漏**：02 交接节 §五 有完整待办表，分 A（立刻能做）/ B（等 VO 收工）/ C（等训练跑完）/ D（等 user 拍板）。

【几条不变的纪律】
· 对 user 一律说人话、通俗中文（`CLAUDE.md` §2）。
· 🔴 **写作红线**（`CLAUDE.md` §0，一个字不能松）：能严证的只有远场单步无碰、每让路步方向合规、
  紧急迫近不可避的可靠判据三条；**绝不写** provably collision-free / 裸 provably compliant / 完整可证明覆盖；
  第四条（可清障集上的前向不变）**仍是暂定**，处处标暂定。
· 篇幅口径是 **20 页上限**（含封面页、结构化摘要、正文、致谢、参考文献、图表），**不是 7500 词**。
  现在正好 20 页 ⟹ 加内容就要同步想砍哪里。user 明示"先别为图表数量做删减，需要时再删"。
· 摘要有硬约束：**≤300 词、独占一页且不含其他内容、五个必需标题、不得含公式或未定义缩写**。
  现在折算约 380–410 词，**超 80–110 词**，转英文时必须再压。
· 所有可视化走 nature-skills 的 `nature-figure` 规范（配色也是）。
· 烧卡必须 user 拍板 + 逐字预检 + screen 后台。
· 别在临时目录里对真仓库敲 `git checkout -- .`；改长 `.tex` 时**锚点必须唯一**、先断言位置再动手
  （上个窗口两次栽在这上面，见 03 L243-续12 补记 6/7）。

【还欠一件事，看到就提醒 user】
通过门 4 与门 5 **至今未过**，缺的是同一个东西：一个"让路密集 / 对抗性场景源"。
它是**纯评估、不训练、不占那 4 台训练机**，可以在会话容器里做。上个窗口推荐做，但 user 还没最终拍板。

────────────────────────────────────────────────────────────
