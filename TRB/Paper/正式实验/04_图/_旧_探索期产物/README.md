# 探索期出图产物（留档，**不是当前结果**）

这四张由 `make_figures.py` 在**探索期臂名册**下生成（金标 / C / A / B / 大集探针），
与正式实验的 9 条臂（TAG 前缀 `F240`）不是同一批数据。

🔴 **不要引用**。正式图待训练收工、`make_figures.py` 迁到正式臂名册后重新生成，
迁移清单见 `04_运行手册.md` §八。

移入此目录的另一个原因：`Fig4_trajectories` 与新生成的 `Fig4_training_reliability`
前缀同名，放在一起容易被当成同一张图的两个版本。
