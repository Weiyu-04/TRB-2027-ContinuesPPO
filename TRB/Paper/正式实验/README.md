# 正式实验 · 定稿方案（`Paper/正式实验/`）

> **这份文件 = 本文件夹的入口，也是正式实验的唯一权威。** 新窗口接手先读它。
> **状态**：🟢 **v3 · 4 项审计已回填 · 工具链就位 · 等服务器冒烟确认后即可起跑**。
> **与 `结果/` 的关系**：`结果/` = 探索期试过的东西；**本目录 = 论文里要写的那一版**。两边物理分开，互不搬运。
> 背景与成本推算见同目录 `方案讨论_v1.md`（讨论稿，非权威）。

---

## §1 一句话

在 **CommonOcean 官方场景库**上，用 **10M 步、5 颗固定种子**，把「连续动作 + 投影式安全盾」与
「对标论文的离散动作 + 动作屏蔽」及三条基线放在**同一训练集、同一测试集、同一套指标**下比一次，
并用**三臂消融**把两个机制的贡献拆开。

---

## §2 已拍板的口径（user 2026-07-29 定 · **定稿后不再改，改了就得全部重跑**）

| 项 | 定稿值 | 备注 |
|---|---|---|
| **训练集** | 官方 **1400/600** 划分不动；1400 再切 **1300 训练 + 100 验证** | 训练与训练期评估**全程不碰**测试集 |
| **报数集** | 官方**测试 600**（剔泄漏后的实际分母见 §4 ⚠️）| |
| **步数** | **10.16M**（20 段 × 507,904），从零一次端到端 | 不用热启动。20 段 ⟹ 与既有 5.08M 臂**同网格**、可叠图；20 个存档候选 |
| **种子** | **固定 0–9，共 10 颗** | 固定集合。n=10 ⟹ 配对符号检验最小 p = 0.002（n=5 时是 0.0625，永远到不了 0.05）|
| **存档口径** | 用那 **100 个验证场景**挑**最佳存档** | 🔴 **绝不能用测试集挑**（= 拿考题选答案）。所有臂一视同仁 |
| **分段存档** | `STEP4E_KEEP_SEGMENTS=1` 全程开 | 每 50 万步留一份，事后可回看任意时刻 |
| 训练期评估 | 那 100 个验证场景 | 曲线即验证曲线 ⟹ 选存档不需要额外跑评估 |

---

## §3 臂集合（9 条 × 10 颗种子 = 90 个 run · 脚本 `代码/run_formal_2027.sh`）

🔴 **三对单变量对子**（这是本次设计的核心，起跑前审计逼出来的）：
| 对子 | 只差什么 | 干净回答 |
|---|---|---|
| **⑤U-无盾 ↔ ⑥U-有盾** | **只差盾** | 盾在连续动作空间里值多少 |
| **③Base ↔ ⑤U-无盾** | 只差动作空间（奖励口径逐字相同）| 离散换连续本身值多少 |
| **⑦都不改 ↔ ⑧只Beta / ⑨只状态机 / ①两个都上** | 只差那两把钥匙 | 两个机制各自的贡献 |

⚠️ ⑤⑥ 用的是**极简配方**（高斯 / paper / 零塑形）—— 因为代码守卫规定无盾臂只能这样（`run_step4e.py:223/233/243`）。
所以"盾值多少"这个答案是**在极简配方下**得到的，不是在完整配方下。论文如实写明。
（此前方案里"我们的配方去掉盾"那种设计**做不到**，硬做会一次差 4 个变量，比 `03` L234 订正⑤批过的还脏。）



| # | 臂 | 作用 | 动作空间 | 盾 |
|---|---|---|---|---|
| 1 | **Ours**（有界 Beta 分布 + 对称让路入口 + 投影盾）| **主角** | 连续 | 投影盾 |
| 2 | **Discrete-safe**（对标论文）| 唯一正面对手 | 离散 49 格 | 动作屏蔽 |
| 3 | Base | 上界参照 | 离散 | 无 |
| 4 | Rule-reward | 钉死「软奖励换不来合规」 | 离散 | 无（软奖励）|
| 5 | **连续·无盾** | 🔴 **项目至今从没训过** —— 补上才能把「离散↔连续」与「有盾↔无盾」两个变量分开 | 连续 | 无 |
| 6 | 消融·都不改 | 消融基准（= 旧配方）| 连续 | 投影盾 |
| 7 | 消融·只 Beta | 拆机制 | 连续 | 投影盾 |
| 8 | 消融·只改状态机 | 拆机制 | 连续 | 投影盾 |

**外部对比算法**（VO / CBF / PD·不训练）：见 §7，口径对齐方案审计中。

---

## §4 ⚠️ 报数分母会变（必须确认后写死）

以往 56 条臂都在**小集**上训练，而小集的 94 个训练场景与官方测试 600 有交集 ⟹ 剔掉 37 个 ⟹ **strict 563**。

**本次全部臂改在官方 1300 上训练，而官方 1300 与测试 600 零交集** ⟹ 理论上**没有任何臂贡献泄漏** ⟹ 分母应为 **600**。

🔴 **两个后果**：
1. 正式实验的分母是 **600**，与历史探索期的 **563** **不是同一个分母** ⟹ **两边的数绝对不能放进同一张表**。
2. 论文里凡引用探索期结论（例如"我们在小集上跑过 10 颗种子"），**必须标明那是另一套口径**。

（此条待代码审计确认后写死。）

---

## §5 指标（**起跑前必须全部就位**）

### 5.1 分两类，优先级完全不同

- 🔴 **A 类·训练期量** —— 跑完就没了、**事后补不回来** ⟹ 起跑前必须齐
- 🟡 **B 类·评估期量** —— 只要存档在，重评一遍就能补（1~2 小时）⟹ 不阻塞起跑

### 5.2 现状

**A 类（训练期）**
- 每 50 万步：到达率 / 碰撞率 / 违规每局 / 紧急步% / 每局时长（= 验证曲线，也用来挑存档）
- 每 ~5 万步的 PPO 内部量：策略损失 · 值函数损失 · 熵 · KL · 裁剪比例 · 学习率 · 每局回报
  · 🆕 **explained_variance**（值函数健康度·崩塌先行信号）· 总损失 · clip_range · 更新次数
  〔后四项 2026-07-29 补记，此前连续臂漏采，离散臂本来就有〕

**B 类（评估期）**
- 对标论文 Table III 五列**全覆盖**：到达率 / 碰撞率 / 违规每局 / 紧急步% / 每局时长
- 违规**拆成让路 + 直航**两项
- 六种态势（无冲突/直航/对遇/交叉/追越/紧急）的步数分布 · 让路对数
- 控制质量：转艏增量（总/让路步/其他步分开）· 油门增量 · jerk · 控制努力 · 次网格细调率 · 速度反转次数
- 安全裕度：CPA 中心距 / 净空 / 发生时刻 · 最近点航向误差 / 速度 · 最大速度
- 盾的行为：介入步数 · 兜底触发% · 投影修正量（均值 / p95 / 最大 / 零修正比例）
- 效率：路径长 · 每局时长 · 最小目标距
- **按会遇类型（对遇 / 交叉）拆全部指标** · **失败原因分型**（超时/出界/停住/碰撞/到达）
- 指定场景的完整轨迹

**已知缺口**：单步求解耗时（实时性）—— B 类，事后补测即可。
**其余缺口待指标审计回填（§9）。**

---

## §6 表与图

**表**（脚本在 `03_表/`，只读原始产物、不许手抄）
| 表 | 内容 |
|---|---|
| 表 1 主表 | 臂 / n / 练成的种子数 / 到达率+95%区间 / 碰撞率+碰撞局数 / 违规（总=让路+直航）/ 转艏增量 / 油门增量 / 紧急步% / 单步求解耗时 · **附逐种子原始值**（n=5 时 5 个数直接摆出来比宽区间更有说服力）|
| 表 1b | 同表，仅算练成的种子（`03` L234-E④：崩种子污染逐局平均，且污染方向每条臂不同）|
| 表 2 消融 | 四臂 × 转艏增量 / 让路违规 / 到达率 / 练成种子数，**同种子配对** |
| 表 3 分型 | 按会遇类型（对遇 / 交叉）拆主表几列，数据已在记、出表零成本 |
| 表 4 外部基线 | VO / CBF / PD，口径见 §7 |

**图**（脚本在 `04_图/`，遵循 nature-figure 规范·静态预检 14 项全过）
Fig.1 样本效率（学习曲线 + 同种子配对 + 收敛性）· Fig.2 消融 · Fig.3 安全-效率权衡（钱图）· Fig.4 轨迹对比。

**统计**（脚本在 `05_统计/`）：同种子配对符号检验 · 按种子重采样的自助法置信区间 · 碰撞用 Fisher 精确检验。全部自算不调库。

---

## §7 外部对比算法（VO / CBF / PD）

不训练、无种子。**口径能否与 RL 臂并表，审计中（§9）**。三种可能的处理，按优先：
1. **完全对齐**（同场景池、同分母、同指标）⟹ 进主表
2. **部分对齐**（同场景池不同分母）⟹ 单独一张表 + 标明分母
3. **不对齐** ⟹ 只做定性对照，写进正文不进表

---

## §8 必须写进论文的诚实口径（不是可选项）

1. **训练预算标注**：所有表标注「10M 步预算 · 验证集选最佳存档 · 官方测试集」。
   若到期仍未收敛（`03` L238 已见先例），只能写「该预算下尚未收敛」，**绝不能写「大数据集效果更差」**。
2. **崩种子污染**：凡逐局平均的指标同时报「全部种子 / 仅练成的种子」两版。
3. **盾的倍数用单变量版**：同为离散、只差有没有盾（`03` L234-E⑤）。跨动作空间那版只能说「我们这一套比无盾基线安全 N 倍」。
4. **碰撞如实报**，写「与对标统计上无差异（Fisher p=…）、二者均远低于无盾基线」，**绝不写「零碰撞」**。
5. **可证明层守 `CLAUDE.md` §0 诚实档**：能严证 = 远场单步无碰 + 每让路步方向合规 + 紧急迫近不可避 sound 证书；**绝不写 provably collision-free / provably compliant（裸）**。命题 4 仍 PROVISIONAL。
6. **COLREGs 覆盖边界**：R17(a)(ii)/(b)/(c)、R18、R19、R9、R10 未实现（对标论文同样没做·范围一致）；**官方库无追越场景**。
7. **探索期结果若引用（如"小集上跑过 10 颗种子"），必须标明是另一套口径**（见 §4）。
8. 🔴 **转艏平顺度不能写成"我们赢了离散臂"**：我们的连续臂训练时开着 `r_rate`，罚的**正是**上报的
   「转艏增量」这个量，而离散臂结构上拿不到这一项（`reeval_official.py:561` 早已立此红线）。
   ✅ 正确写法 = **消融口径**："同样开着该罚项，无界高斯 0.01472 → 有界 Beta 0.00403 ⟹ 改善来自**有界动作分布**，
   不来自额外奖励项（不换分布时这个罚项打不赢离散基线）"。跨臂只能写"我们这一套比基线平顺 N 倍"。
   同理 `PARK_W=20`（终端保速势）帮到达率，离散臂也没有 ⟹ 一并披露。
9. 🔴 **不得暗示我们比几何控制器（CBF/VO/PD）平顺**：实测 CBF 最优配置 jerk 0.165 / 转艏 0.00231，
   我们 0.196 / 0.00256 —— **基本打平，略输**。正确说法："我们学出来的策略平顺度**与手工调参的几何控制器相当**，
   同时到达率高 22 点、违规低 9 倍"。这比"我们最平顺"稳得多。
10. **外部基线的到达率必须带限定**：它们位置命中 97~99.6%，卡在终端**朝向门 ±9.74°** 外（朝向误差 15~32°）；
    几何控制器的标称律不含终端对准环节 ⟹ 那一列反映的是终端约束类型差异，不是避碰能力。
11. **外部基线的违规数受回合长度影响**：它们未到达局精确跑满 170 步，我们约 80 步，而直航违规是逐步累计的
    ⟹ 必须同时报"每局"和"每步"两版。

---

## §9 🟡 进行中的 4 项审计（回填后本文件转 v3 定稿）

4 项审计全部完成，关键结论逐条核过代码。**已落地的修复**：

| 审计发现 | 处置 |
|---|---|
| 训练 rollout 的 `info` 从没被读过（A 类量·补不回）| ✅ `_RolloutStats` 已落地（`03` L239）· 25 项回归全过 |
| 两臂 curves schema 不对称 | ✅ 已对齐（连续臂补 `ret_rms_var`，离散臂补 4 项）|
| 「验证集挑最佳存档」工具不存在 | ✅ `代码/tests/select_best_ckpt.py` 已建（规则见 §2b）|
| 分母写死 563（4 处，其中 3 处是字面量、不报错会把错数字印进论文）| ✅ 全部改成按实测/显式声明 |
| `run_ds_reseg.sh` 的 `rm -rf` 会删掉全部 run 的分段副本 | ✅ 已改成只删本次冒烟的 TAG |
| 连续无盾臂被守卫锁死、做不成它的任务 | ✅ 改成 ⑤⑥ 孪生对（见 §3）|
| 「转艏赢 5.1×」与项目自己的红线冲突（`r_rate` 直接罚了那个指标）| ✅ 改成消融口径（见 §8-8）|
| n=5 撑不起配对检验与训练可靠性两条声明 | ✅ 改用 10 颗种子 |
| 最佳存档的选择偏倚 ≈ 6~7.5 点 | ✅ 强制两版都报（见 §2b）|
| 外部基线：口径基本已对齐、调参**没碰测试集**（独立验证）| 建议重跑一趟同口径（1 小时·纯评估）|
| 外部基线 CBF 在平顺度上略优于我们 | 见 §8-9 |

### §2b 存档选取规则（**先定死·不带任何旋钮**）
1. 候选 = 全部 20 段 · 2. **单一指标 = 验证集到达率**，取最高 · 3. **平局取更早的段** · 4. 完。
🔴 **不加任何硬门槛** —— 阈值就是旋钮，会被问"这个数怎么来的"。工具会把"选中段在碰撞/违规上明显差于末段"**报警打出来**（规则不变，但人必须看见）。
🔴 **必须两版都报**：验证集 N=100 ⟹ 到达率二项标准误 ≈ 4 点；20 个候选取最大，期望上抬 ≈ **7.5 点**，
比我们专门立规矩防的 1.6pt 跨趟抖动大 4 倍多。⟹ 最佳存档一趟 + 末段存档一趟，论文两版都报并量化交代。

---

## §10 时间账（按实测 fps 外推 · 官方 1300 上连续臂约 0.76 h/百万步）

| 项 | 估算 |
|---|---|
| 90 个 run × 10.16M | 总机时 ≈ **605 小时**；每台 32 核 ÷ 每 run 8 环境 = **最多 4 路**（不是 6 路）⟹ **两台并行 ≈ 3.2 天** |
| 同趟重评（全部臂 + 外部基线）| 1~2 小时 |
| 出表出图 | 分钟级（脚本已就位）|

**余力选项**（跑完主线若还有时间）：主线补到 10 颗种子；再有余力才补其他臂。

---

## §11 目录

| 子目录 | 放什么 |
|---|---|
| `01_训练产物/` | 每条臂的 jsonl / log / checkpoints（**含 `segments/`**）|
| `02_重评产物/` | 同趟重评的 `g*.json` / `g*.log` / `g*_traj.json`，一趟一个子目录 |
| `03_表/` | 表 + **生成脚本** |
| `04_图/` | 图 + **生成脚本**（见该目录 README：绘图规范落地清单）|
| `05_统计/` | 检验/区间脚本与输出 |

**铁律**：每张表图必须有生成脚本、脚本只读原始产物、**不许手抄数字**；每条臂必须同机同趟评；训练期里程碑数永不进报数表。
